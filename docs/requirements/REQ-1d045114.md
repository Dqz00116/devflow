---
id: REQ-1d045114
title: DevFlow Core Robustness Improvements
status: done
priority: high
---

## Description

Fix critical robustness issues in DevFlow v2.0 identified during codebase audit. This addresses
gate checker security, workflow engine type safety, and config defaults.

## Acceptance Criteria

- [ ] Unknown gate types fail instead of silently passing (security fix)
- [ ] `WorkflowEngine.advance()` uses a proper result type (no string-matching for "complete")
- [ ] `Config.version` default changed from hardcoded "0.1.5" to empty string
- [ ] All existing tests pass with updated interfaces
- [ ] No regressions in CLI behavior

## Notes

- P0 fix: `gate_checker.py:259` currently returns `True` for unknown gates — must return `False`
- P1 refactor: `advance()` returns `(bool, Step | None, str)` with 4 semantic overloads
- P1 fix: `config.py:21` hardcodes tool version as project version default
