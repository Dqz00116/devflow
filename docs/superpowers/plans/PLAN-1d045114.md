---
id: PLAN-1d045114
title: Core Robustness Improvements — Implementation Plan
status: draft
---

## File Structure

No new files. Changes to existing modules only:

```
src/devflow/
├── config.py              # Fix 3: version default
├── gate_checker.py        # Fix 1: unknown gate fail
└── workflow_engine.py     # Fix 2: AdvanceResult dataclass + refactor
src/devflow/
├── cli.py                 # Fix 2: update caller
└── loop_engine.py         # Fix 2: update caller
tests/
├── test_gate_checker.py   # Add test for unknown gate
├── test_workflow_engine.py # Update for AdvanceResult
└── test_loop_engine.py    # Update for AdvanceResult
```

## Tasks

### T-001: Fix Config.version default
- **File**: `src/devflow/config.py` line 21
- **Change**: `version: str = "0.1.5"` → `version: str = ""`
- **Verify**: existing tests pass, release script unaffected

### T-002: Add AdvanceResult dataclass
- **File**: `src/devflow/workflow_engine.py`
- **Change**: Add `@dataclass AdvanceResult` with fields: `advanced`, `current_step`, `message`, `is_complete`
- **Verify**: module imports cleanly

### T-003: Refactor advance() to return AdvanceResult
- **File**: `src/devflow/workflow_engine.py` — `advance()` method
- **Update 5 return sites** in the method body to use `AdvanceResult`
- **Verify**: unit tests pass

### T-004: Update CLI caller
- **File**: `src/devflow/cli.py` lines 443-455 — `done()` command
- **Change**: destructure `AdvanceResult` instead of tuple + `"complete" in msg.lower()`
- **Verify**: CLI integration tests pass

### T-005: Update LoopEngine caller
- **File**: `src/devflow/loop_engine.py` lines 151-164 — `run()` method
- **Change**: destructure `AdvanceResult` instead of tuple
- **Verify**: loop engine tests pass

### T-006: Fix unknown gate to fail
- **File**: `src/devflow/gate_checker.py` line 259
- **Change**: `return True, f"Unknown gate (skipped): {gate}"` → `return False, f"Unknown gate type: {gate}"`
- **Verify**: test passes, any workflow with malformed gates now correctly blocks

### T-007: Add unknown gate test
- **File**: `tests/test_gate_checker.py`
- **Change**: add test case: `check_gate("bad_gate:foo", ...)` → `(False, ...)`

## Test Strategy

- **Unit tests**: Update `test_workflow_engine.py` for new `AdvanceResult` type; add unknown gate case to `test_gate_checker.py`
- **Integration tests**: `test_cli.py` subprocess tests verify CLI behavior unchanged
- **Regression check**: `pytest` full suite must pass GREEN before advancing
