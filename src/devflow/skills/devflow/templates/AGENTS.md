# Project Context

This file provides context for AI agents working on this project.

## Tech Stack

**Language**: {{ project.language }}
**Stack**: {{ project.stack }}
**Version**: {{ project.version }}

## Constraints

{% if constraints.zero_warnings %}
- **ZERO warnings** - All builds must have zero warnings
{% endif %}
{% if constraints.zero_mocks %}
- **ZERO mocks** - Use real instances only in tests
{% endif %}
{% if constraints.nullable %}
- **Nullable enabled** - Use nullable reference types
{% endif %}

## Project Structure

```
{{ project.name }}/
├── .devflow/              # DevFlow configuration
│   ├── config.toml
│   └── state.toml
├── {{ paths.src }}/       # Source code
├── {{ paths.tests }}/     # Tests
├── {{ paths.docs }}/      # Documentation
│   ├── WORKFLOW.md        # Development workflow
│   ├── REQUIREMENTS.md    # Requirements guide
│   ├── CLI.md             # CLI reference
│   ├── requirements/      # Requirement files
│   │   ├── TEMPLATE.md
│   │   └── REQ-*.md
│   ├── features/          # Feature implementation docs
│   │   ├── TEMPLATE.md
│   │   └── FEAT-*.md
│   └── superpowers/       # Design docs & plans
│       ├── specs/
│       └── plans/
└── AGENTS.md              # This file
```

## Key Commands

```bash
# Build
{{ commands.build }}

# Test
{{ commands.test }}
{{ commands.test_unit }}
{{ commands.test_integration }}

# Lint
{{ commands.lint }}
```

## Development Workflow

See [docs/WORKFLOW.md](docs/WORKFLOW.md) for the complete development workflow.

Quick reference:
1. **Stage 0**: Create/approve requirement (`devflow req new REQ-001`)
2. **Stage 1**: Brainstorming (design doc)
3. **Stage 2**: Writing plans (implementation plan)
4. **Stage 3**: Implementation (TDD + subagents)
5. **Stage 4**: Code review
6. **Stage 5**: Testing
7. **Stage 6**: Verification
8. **Stage 7**: Finish

## Coding Standards

- Follow existing code patterns
- Write tests before implementation (TDD)
- Keep functions small and focused
- Use meaningful variable names
- Add docstrings for public APIs
- Run linting before committing

## AI Agent Guidelines

When working on this project:

1. **ALWAYS** read `using-superpowers` skill first
2. **ALWAYS** check requirements before starting (`devflow req list`)
3. **ALWAYS** follow TDD (test before code)
4. **ALWAYS** verify before claiming done
5. **ALWAYS** update requirement status when complete

Use the DevFlow CLI to track work:
```bash
devflow status      # See current project status
devflow validate    # Check workflow compliance
```

---

*Generated by DevFlow v1.0*
